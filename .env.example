# ============================================================================
# ZerxLab Website - 环境变量模板
# ============================================================================
# 使用说明:
#   1. 将此文件复制为 .env (本地开发) 或构建 CI 的环境变量
#   2. 填入真实值后保存
#   3. .env 已加入 .gitignore,不会被提交到仓库
#
# 站点现为全静态构建(output: "static"),构建期一次性读完 src/content/**,
# 运行时不再需要任何环境变量 —— 下面这些全部只在 `bun build` / `bun dev` 时生效。
# ============================================================================


# ---------- 站点 ----------

# 站点公开 URL,用于生成 sitemap / RSS / OG meta 的绝对地址
# 本地开发可留默认,生产部署(GitHub Actions)时设置为真实域名
SITE_URL=https://zerx.dev


# ---------- 可选:构建时数据源 ----------

# GitHub Personal Access Token (可选)
# - 用途: 构建时拉取 zerx-lab 组织的仓库 stars/forks/language 等真实数据
# - 权限: 只需要 public_repo 读权限(最小权限,只读公开仓库元数据)
# - 创建位置: https://github.com/settings/tokens?type=beta (Fine-grained 推荐)
# - 不配置时:直接使用 src/content/data/projects.json 里手填的 stars/forks 快照,
#   不影响构建成功,只是数字不是实时的
# GITHUB_TOKEN=


# ---------- 可选:giscus 评论 ----------

# 四项缺一即视为未配置,评论区组件(src/components/blog/Comments.astro)
# 会静默跳过、不渲染任何内容,不报错、不影响构建
# 配置向导: https://giscus.app/zh-CN

# 存放 Discussions 的仓库,格式 owner/repo
# PUBLIC_GISCUS_REPO=

# 上面仓库的 GitHub ID(giscus.app 页面自动生成的 data-repo-id)
# PUBLIC_GISCUS_REPO_ID=

# Discussion 分类名(建议单独建一个 "Comments" 分类,Announcement 类型)
# PUBLIC_GISCUS_CATEGORY=

# 上面分类的 ID(giscus.app 页面自动生成的 data-category-id)
# PUBLIC_GISCUS_CATEGORY_ID=
