---
/**
 * ============================================================================
 * 关于页 (英文)
 * ----------------------------------------------------------------------------
 * 路由:
 *   /en/about  → 本文件(en)
 *   /about     → src/pages/about.astro(默认中文,无前缀)
 *
 * 实现:
 *   与中文版完全同构,共用 AboutPage 组件。
 *   AboutPage 内部通过 getLangFromUrl(Astro.url) 推断当前语言,
 *   URL 以 /en/ 开头时自动输出英文文案(About / In Numbers / Join us on / ...)。
 *
 * 数据源:
 *   - FALLBACK_SITE_SETTINGS / FALLBACK_LANGUAGE_STATS / FALLBACK_PROJECTS
 *   - 未来接入 Directus 后可替换为真实 site_settings 单例拉取
 *
 * 维护约定:
 *   若要修改关于页的结构/样式/数据拉取逻辑,只改
 *   src/components/AboutPage.astro,本文件与中文入口都会受益。
 * ============================================================================
 */

import AboutPage from "@components/AboutPage.astro";
---

<AboutPage />
