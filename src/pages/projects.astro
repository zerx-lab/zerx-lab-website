---
/**
 * ============================================================================
 * 项目页 (中文,默认无前缀)
 * ----------------------------------------------------------------------------
 * 路由:
 *   /projects     → 本文件(zh)
 *   /en/projects  → src/pages/en/projects.astro(英文)
 *
 * 实现:
 *   全部逻辑抽到 ProjectsPage 共享组件,本文件仅做入口。
 *   页面中的语言由 Astro.url 推断(ProjectsPage 内部调用 getLangFromUrl)。
 *
 * 数据源:
 *   - src/content/data/projects.json(构建期读取)
 *   - GitHub API 的 stars / forks(可选,拉不到就用内容里的静态值)
 *
 * 维护约定:
 *   若要修改项目页的结构/样式/数据拉取逻辑,只改
 *   src/components/projects/ProjectsPage.astro,本文件与英文入口都会受益。
 * ============================================================================
 */

import ProjectsPage from "@components/projects/ProjectsPage.astro";
---

<ProjectsPage />
