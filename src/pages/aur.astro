---
/**
 * ============================================================================
 * AUR 页 (中文,默认无前缀)
 * ----------------------------------------------------------------------------
 * 路由:
 *   /aur     → 本文件(zh)
 *   /en/aur  → src/pages/en/aur.astro(英文)
 *
 * 实现:
 *   全部逻辑抽到 AurPage 共享组件,本文件仅做入口。
 *   页面中的语言由 Astro.url 推断(AurPage 内部调用 getLangFromUrl)。
 *
 * 数据源:
 *   - src/content/data/aur.json(构建期读取)
 *   - AUR RPC v5 的实时 version(可选,拉不到就用内容里的静态值)
 *
 * 维护约定:
 *   若要修改 AUR 页的结构/样式/数据拉取逻辑,只改
 *   src/components/aur/AurPage.astro,本文件与英文入口都会受益。
 * ============================================================================
 */

import AurPage from "@components/aur/AurPage.astro";
---

<AurPage />
